/** @format */

import React from "react";

const Loading = () => {
  return (
    <div className="flex justify-center p-5">
      <button className="btn loading text-primary">loading</button>
    </div>
  );
};

export default Loading;
